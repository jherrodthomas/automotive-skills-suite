# Week 2026-W37 — Plan

_Generated 2026-09-07 (Monday) by automotive-skills-daily-standup (PLAN mode)._

## Recap — past 7 days

```
a92088c auto(triage): 12 issues reviewed, 5 stale commented, no low-confidence labels applied
2adc232 auto(release): cut v2026.09.W36, two-week tag, seven reviewer crashes fixed
b5fa14f auto(docs): W35+W36 changelog rolled, four mbse/sysml reviewer stubs, drift logged
da1082a auto(polish): #58 mbse first pass, three crashing mbse reviewers fixed, stakeholder needs row resolved
72eefda auto(polish): #57 sysml-block-diagram gets JSON input, placeholders out, BDD-12 symmetric
357da3d auto(monthly): KPI report for August 2026
5660048 auto(polish): #52 traceability pair non-functional, reviewer passes empty workbook
30044b2 auto(plan): W36 slots deferred #52 first, opens sysml input and mbse first pass
```

**W36 landed all three targets — the third clean week in a row.** #52 (traceability first pass,
Tue), #57 (sysml-block-diagram JSON input, Wed), #58 (mbse first pass, Thu). The fortnight also
cut `v2026.09.W36`, the first tag since W34.

The week's real yield was not the targets, it was the method: **executing a reviewer against its
builder's output found seven reviewers that crashed on every input** — four sysml (#56), three
mbse (#58) — all the same one-line `dashboard.py` defect. No filename-level or prose-level check
would ever have seen it. That is now the first thing a first pass does.

## Audit run before slotting — one hypothesis killed

The W35→W36 logs carried a repo-wide suspicion into this plan: that `dashboard.py` gates its
`REJECTED` verdict on `obligation == "Shall"` while `check_definitions.py` uses `Must`, making
`REJECTED` dead code **across all 76 reviewers**. That was flagged three times as a likely
high-payoff batch pass. It was cheap to measure, so it was measured before it was slotted.

Scan of all 76 reviewer archives (`dashboard.py` vs `check_definitions.py`):

| Class | Count | Meaning |
|---|---|---|
| Dashboard gates on `Shall`, checks use only `Must` — **`REJECTED` genuinely dead** | **3** | `mbse-model-architecture`, `mbse-requirements-allocation`, `mbse-system-context` |
| Dashboard gates on `Shall`, checks use `Shall` — consistent | 38 | no defect |
| `dashboard.py` carries no `Shall` literal at all | 11 | all autosar / sysml / cdd / dtc-catalog — different verdict shape, not this bug |
| Neither `Must` nor `Shall` literal in `check_definitions.py` | 24 | obligation is carried elsewhere; needs its own look, not this fix |

**The hypothesis is false.** It is not suite-wide, it is three archives, and all three are mbse —
so it collapses into target 2 as a three-line add rather than occupying a slot of its own. Killing
it costs the week nothing and buys back a day. The 24 "neither" reviewers are a separate open
question and are carried, not slotted.

## This week's targets (3)

Rule (a) supplies nothing actionable: all 12 open issues (#43–#52, #57, #58) are recorded as
DoD-met and are waiting on the human close queue, which four consecutive plans have now cited.
Rule (b) is skipped — 0 orphan builders, alias registry honored. So the table is built from
rule (c) plus the three verified W36 follow-ups.

| # | Issue | Skill(s) | Domain | Definition of done |
|---|---|---|---|---|
| 1 | [#59](https://github.com/jherrodthomas/automotive-skills-suite/issues/59) | traceability-matrix-builder + reviewer | v&v | Repair **in dependency order**: input-to-tabs wiring first (the builder currently ignores its input and the reviewer passes an empty workbook), then TM005 / TM006 / TM007 in the same change once the tabs carry real data. Verified by building from `examples/traceability-matrix-builder/sample_input.json`, running the reviewer over that output, and recording the FC/PC/NA spread. A cell scan must hit a value that exists **only** in the sample input. |
| 2 | [#60](https://github.com/jherrodthomas/automotive-skills-suite/issues/60) | mbse-system-context-builder (+ 3 mbse reviewers) | mbse | Wire `stakeholder_needs`, `boundary`, `assumptions`, `references`, `document_control` into their tabs; drop or document `Validation Rules`; fix the SKILL.md example (`actors` vs `primary_actor`) and the advertised tab count in the same change. **Plus the folded audit fix:** align the obligation vocabulary in the three mbse reviewers so `REJECTED` can fire. Verified by a build that makes `CR05` reachable and a reviewer run that reports `REJECTED` on a deliberately failing workbook. |
| 3 | [#61](https://github.com/jherrodthomas/automotive-skills-suite/issues/61) | test-case-catalog + flexray-config (builder/reviewer pairs) | v&v · comms | Twice-carried. For each pair, confirm by execution whether the one-name sheet mismatch is a #55-class mechanical fix or a real chain break. Mechanical → fix inline and regenerate `docs/chain-contract-audit.md`. Real → file with evidence and stop. Timeboxed to ~20 minutes each; if both are mechanical the day also picks up the carried `Bus Interfaces` check in `autosar-bsw-config`. |

Slotting: **Tue → #59. Wed → #60. Thu → #61.**

**#59 goes first and its DoD is unchanged from the W36 follow-up.** It is a carried repair with a
written dependency order, and W34's proven lesson is that a carried item slotted first ships while
a carried item slotted last does not. #60 is Wednesday because it is the largest authoring change
on the table — five tabs plus a doc fix — and because the audit above only just settled its scope.
#61 is Thursday because it is two timeboxed confirmations, which is the right shape for the day
most likely to be squeezed.

## Domain-spread check

W35 was autosar + sysml. W36 was v&v + sysml + mbse. W37 as slotted is v&v + mbse + (v&v · comms).
**That is three consecutive weeks weighted to the modeling and V&V cluster, and it is deliberate
this once** — every item above is a defect with verified evidence behind it, and defects outrank
rotation. But it is now a standing imbalance: safety, cyber, quality, calibration, aspice,
diagnostics and program-mgmt have not been entered since W30–W33, and 68 of 76 builders are 🟡
stale (30+ days).

Two consequences written down here so the next plan is held to them:

1. **The sysml JSON-input batch (`activity`, `requirement`, `state-machine`) is deferred to W38**,
   not because it is unready — #57 proved the pattern and left `_g()` / `write_rows()` to reuse —
   but because a fourth consecutive sysml/mbse week for an *enhancement* while seven domains sit
   untouched is the wrong trade. It is an enhancement with a proven pattern and no urgency.
2. **W38 takes at least one target by rule (c) from outside the modeling cluster**, drawn from the
   2026-05-01 cohort (`hara`-adjacent safety, `tara`/cyber, quality). Rotation is not optional two
   weeks running.

## Carried, not slotted

- sysml JSON-input batch (`activity`, `requirement`, `state-machine`) reusing the #57 helpers — **W38**.
- The 24 reviewers whose `check_definitions.py` carries neither `Must` nor `Shall`: find where
  obligation actually lives before assuming a defect. Today's scan says this is a real unknown, not
  a known bug.
- `regen_status.py` → `max(builder, reviewer)` for `Last Touched`, so reviewer-only repairs move a
  row. One line; deliberately not taken today — STATUS is a contract two other modes read, and the
  column change should land with its own written note rather than inside a PLAN commit.
- `scripts/` invariant lint, five invariants, the fifth being *every reviewer's
  `generate_checklist.py` runs to completion on its builder's `{}` output* — which this fortnight
  proved would have caught all seven crashes.
- `examples/mbse-model-architecture-builder/` and `examples/mbse-requirements-allocation-builder/`
  still lack `sample_input.json`; both reviewers verified only on `{}` output.
- `Inter-Module Dependencies` / `Validation Rules` schema decision; #46 re-scope decision (awaiting
  a human since 09-01).
- Standing item 9 (advertised vs shipped check counts) now spans at least 8 reviewers, roughly 100
  advertised checks that do not exist.

## For the human

- `v2026.09.W36` is tagged and pushed but **not published** — review `RELEASES.md` and click Publish.
- **The close queue is the highest-value action available and is now distorting this plan's ranking
  rule for the fifth week running.** All 12 open issues are DoD-met; #48 and #49 have been open six
  weeks and passed over by five consecutive plans. Close them or say why not.
- Duplicate v&v labels exist: `v-and-v` (in use) and `vv` (unused). Delete one — autonomous runs
  will not merge labels.
