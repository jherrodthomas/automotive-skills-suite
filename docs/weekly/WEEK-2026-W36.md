# Week 2026-W36 — Plan

_Generated 2026-08-31 (Monday) by automotive-skills-daily-standup (PLAN mode)._

## Recap — past 7 days

```
6e8cce6 auto(polish): fix #56 sysml probe crash, 4 reviewers repaired, domain gaps logged
656bdfe auto(polish): fix #55 autosar batch, recalc 152/152, bad heading 0/152
84ea8b4 auto(polish): fix #54 bsw-config input drop, C005/C006 live, all mandatory FC
```

**W35 landed all three targets — the second clean week in a row, and the first back-to-back pair
in the repo's history.** Three targets, three polish days, three shipped: #54 (bsw-config input
drop), #55 (autosar recalc batch), #56 (sysml probe crash). The W34 scoping lesson now has two
weeks of evidence behind it rather than one.

**The bigger news is not in the commit log: the human closed #53 through #56.** The open count
moved 12 -> 10 and, for the first time since W30, the four issues this task opened most recently
are gone from the board. That is a direct response to five weeks of standing-item-1 nagging and it
changes what Monday costs — see below.

Two findings from W35 shape this plan. First, the #56 pass proved that "confirm the pairing by
actually opening a builder output" is the highest-yield check in the first-pass template:
`block_probe.py` had been collecting bound `str.title` methods and dying before check one, on
every input, since the file was written. The reviewer had **never** worked, and no filename-level
check would ever have seen it. Second, the same pass specified — but deliberately did not do —
the real sysml work, and that specification is now target 2 below.

## This week's targets (3)

Three targets for three POLISH days. **For the first time, priority rule (a) ran without a
hand-filter.** With #53–#56 closed, the 10 remaining open issues are #43–#52, and the 2026-08-23
triage records exactly one of them as not-DoD-met: **#52**. So rule (a) supplies target 1 cleanly
instead of returning ten finished issues. Rule (b) is skipped (0 orphan builders, alias registry
honored). Rule (c) is not needed — the two W35 follow-ups outrank a least-recently-touched pick,
and both were written with verified evidence behind them.

| # | Issue | Skill(s) | Domain | Definition of done |
|---|---|---|---|---|
| 1 | [#52](https://github.com/jherrodthomas/automotive-skills-suite/issues/52) (deferred from W34 and W35) | traceability-matrix-builder + reviewer | v&v | Unchanged from W34. Standard first pass: frontmatter validated, generator smoke-tested from its own sample input (written if absent), sheet names confirmed Excel-legal, `recalc.py` confirmed at 5,782 bytes / single hash, **reviewer pairing confirmed by opening a builder output**, polish log + `examples/traceability-matrix-builder/README.md` written from the verified run. |
| 2 | [#57](https://github.com/jherrodthomas/automotive-skills-suite/issues/57) (new) | sysml-block-diagram-builder | sysml | `--input <path.json>` accepted and loaded; dead `import json` made live; schema documented in SKILL.md; placeholder data survives only as `examples/sysml-block-diagram-builder/sample_input.json`, not as generator literals; verified end-to-end through the now-working reviewer with the FC/PC/NA spread recorded, and a cell scan hitting a block name that appears **only** in the sample input. BDD-12 port symmetry decided in the same change. |
| 3 | [#58](https://github.com/jherrodthomas/automotive-skills-suite/issues/58) (new) | mbse-system-context-builder | mbse | Standard first pass, same shape as target 1. Defects found are **filed, not fixed inline**, unless one-line mechanical. |

Slotting: **Tue -> #52. Wed -> #57. Thu -> #58.**

**#52 goes first, and this is the whole point of the week.** It was slotted Thursday of W34 and
never started because #51 overran. W35 deferred it rather than re-slotting it, and wrote down the
marker to check: _"if it slips a second time without an overrun to explain it, that is the signal
to descope it."_ It has not slipped a second time — it was deferred once, deliberately, with a
stated reason — so its DoD stays unchanged. But it does not get a third Monday. Applying W34's
proven lesson, **descope and slot first beats carry**, it takes the Tuesday slot ahead of two
newer and more interesting items. If it does not land on Tuesday, the next plan descopes it
without further discussion.

#57 is second because it is the largest genuine defect on the repo and because Wednesday is the
right day for it: it is scoped to **one** builder as a reference implementation, not four. Four
schemas in one pass is exactly the change the task file says to stop short of. The other three
sysml builders follow in W37 once this one settles the schema.

#58 is third and is the cheapest of the three by construction — `generate_context.py` already
loads JSON, so it is a genuine audit rather than an authoring job, and audits are the work that
compresses when a day runs short.

## Domain-spread check

Targets span **v&v, sysml and mbse** — three domains, no doubling. W35 doubled on autosar
deliberately and said so; this week does not need to, because the priority rules landed on three
different domains without being steered.

| Domain | Builders | Last touched | Status after W36 (if targets land) |
|---|---|---|---|
| v&v | 5 | 2026-05-02 (all) | **first pass begun** — the gap open since W30 |
| sysml | 4 | 2026-08-27 (reviewers) | builders accept input for the first time; 3 remain -> W37 |
| mbse | 3 | 2026-05-01/02 | **first pass begun — no domain left never-entered** |

**If all three land, every domain in the suite has been entered into a weekly table at least
once.** That milestone has been the tracked objective since W30 and W36 is the week it closes.
The v&v and mbse passes are firsts; the sysml one is a second visit that reaches the builders
rather than the reviewers.

## Notes

- STATUS regenerated via `scripts/regen_status.py`: **76 builders / 76 reviewers / 100% paired**
  (2 alias pairings), red 0 / **yellow 66** / green 10. Yellow held at 66 and green held at 10 —
  W35's three commits refreshed skills that were already green, so the counts did not move.
  **No skill regressed.** Seventh consecutive run making this observation: the flag measures
  cadence, not health. One skill per polish day cannot hold 76 builders inside a 30-day window;
  the arithmetic ceiling is roughly 13. It will oscillate forever until the window widens to 90
  days or the flag is dropped — standing item 6, outstanding since W30.
- Polish-log coverage: **28 of 76 builders (37%)**, up two on W35. Example READMEs: **47**
  directories. Both W36 first-pass targets add one each.
- #57 and #58 went out with `weekly-target` + domain and **no type label** — sixth consecutive
  week blocked on the same unmade decision (standing item 3). #57 also carries `skill-bug`, which
  *is* a type label in the triage taxonomy, so the two new issues are not even labelled
  consistently with each other. This is small and it is cheap to fix.
- The `/tmp/automotive-work` collision did not recur for a third consecutive run. Follow-up
  dropped as agreed.
- Chain-contract audit unchanged since 2026-08-27: 46 assertions / 16 chains / **0 BREAK**.

## Standing items for the human

1. ~~Close #43 through #52~~ -> **partially done, thank you.** #53–#56 are closed. The remaining
   ten (#43–#52) are still open and nine of them are DoD-met with evidence cited in
   `docs/triage/2026-08-23.md`. **#52 is the only one that should stay open**, and it is this
   week's Tuesday target. Closing the other nine would take the board to a true zero-backlog state
   for the first time.
2. **The 30-day stale rule is still misfiring**, and will keep doing so while nine shipped issues
   sit open. It has posted "closing in 7 days unless updated" on work that shipped in
   `v2026.08.W33`. Either close the nine, or amend the rule to skip DoD-met issues.
3. **Add a `polish-target` label** (or accept `weekly-target` as the type). Sixth week blocked.
   See the labelling inconsistency between #57 and #58 above.
4. **Delete the duplicate `vv` label** — confirmed still present today alongside the in-use
   `v-and-v`. A genuine collision; a future run could plausibly pick either. Confirm unused first.
5. **Decide whether #46 closes or re-scopes.** Its DoD was narrowed in W34 to builder-to-builder
   reads and static analysis, so **column-layout drift remains uncovered**. Closing it as-is is
   defensible; so is a successor issue. It should not sit ambiguous.
6. **Sign off (or reject) the freshness rule change** — outstanding since W30. See the yellow-count
   note above.
7. **Publish the pushed tags** — v2026.07.W29 through v2026.08.W34 are pushed with no GitHub
   Release object (by design; autonomous runs never publish).
8. **The vanished-issues pattern #33–#42** — flagged W30, still unexplained, no new evidence.
9. **New: the three sysml stub reviewers.** `sysml-activity-diagram`, `sysml-requirement-diagram`
   and `sysml-state-machine` each ship a 343-byte `check_definitions.py` with **zero** CheckDefs
   while their SKILL.md advertises 25 checks apiece — they exit 0 and hand back a title-only
   workbook. That is ~75 checks to author. It is real work, it is not a polish-pass change, and
   it is not on this week's table on purpose. It needs a human decision on whether the autonomous
   run should author checks at all.
